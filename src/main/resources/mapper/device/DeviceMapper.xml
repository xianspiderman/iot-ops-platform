<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.xianspiderman.iotops.device.DeviceMapper">
    <select id="selectBySns" resultType="io.github.xianspiderman.iotops.device.Device">
        SELECT id, sn, device_name, project_id, product_id, imei, mac, firmware_version,
               online_status, last_communication_time, created_at, updated_at
          FROM device
         WHERE sn IN
        <foreach collection="sns" item="sn" open="(" separator="," close=")">
            #{sn}
        </foreach>
    </select>

    <insert id="batchInsert">
        INSERT INTO device
            (sn, device_name, project_id, product_id, imei, mac, firmware_version, online_status)
        VALUES
        <foreach collection="devices" item="device" separator=",">
            (#{device.sn}, #{device.deviceName}, #{device.projectId}, #{device.productId},
             #{device.imei}, #{device.mac}, #{device.firmwareVersion}, #{device.onlineStatus})
        </foreach>
    </insert>
</mapper>
