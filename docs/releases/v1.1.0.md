# v1.1.0 — 中文演示与写入基准

## 新增与改进

- 管理端全部页面中文化，同时保留 SN、eventId、requestId、RocketMQ、XXL-JOB 和 OpenAPI 等技术标识。
- 后端枚举、权限编码、API 路径与数据库字段保持不变，前端集中维护中文显示映射和错误码提示。
- 工单页新增超时状态、详情与操作轨迹抽屉，可以直接查看 SYSTEM 超时轨迹。
- 演示数据通过 Flyway V5 中文化，空数据库启动后界面与文档一致。
- 新增 500 条合法设备通过一次 MyBatis XML 多值 INSERT 真实提交的独立基准，分别报告应用侧 Mapper 调用和包含 commit 的事务总耗时。
- 补充管理端、OpenAPI 与 XXL-JOB 的关系说明、正常串联演示和失败恢复演示。

## 验证边界

- 保留原 1,000 行查询基准；其中 `6 ms` 仍只代表 4 次批量查询，不与 INSERT 指标合并。
- INSERT 基准排除实体构造、Excel 解析、查询、校验和清理；10 轮正式样本全部保留在 `docs/testing.md`。
- 后端全量测试、两项独立基准、Node 24 构建与审计、干净 Compose、HTTP 健康检查和三入口串联流程均在发布前重新执行。
